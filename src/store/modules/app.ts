/**
 * 系统通用状态
 */
const state = () => ({});
const getters = {};
const mutations = {};
const actions = {};

const app = {
  namespaced: true,
  state,
  getters,
  mutations,
  actions,
};

export default app;
